Verify the vite production build compiles cleanly and show changes summary for user review.

Input: issue_number (optional)

Steps:
1. Build: npm run build  (vite production build)
2. Check exit code: 0 = success, non-zero = compile/bundle failure
3. Theming check (CSS custom properties only): confirm no hardcoded color values
   were introduced in component CSS. All colors and visual primitives must use the
   CSS custom properties from src/index.css / @derekwinters/design-tokens — never
   hardcoded values (per CLAUDE.md "Theming"). src/__tests__/designTokens.test.js
   enforces part of this; flag any hardcoded color so it is fixed before commit.
4. Prepare changes summary:
   - git diff --stat to get file changes
   - Count modified files
   - Summarize what was changed (ui, api client, theming/tokens, routing, tests/stories, docs)
5. Show summary with:
   - Issue number and title
   - Files modified (with line counts)
   - Summary of changes
   - Test results (from previous step)
   - Build status
6. Pause and ask user:
   - Approve for commit?
   - Request more changes?
   - Abort?

Output format:
┌─────────────────────────────────────────┐
│ Issue #42: Add chore list view          │
│ Verification Complete                   │
└─────────────────────────────────────────┘

Files Modified:
- src/pages/ChoreList.jsx: +85, -0 lines
- src/hooks/useChores.js: +62, -0 lines
- src/api/chores.js: +15, -2 lines
- src/App.jsx: +8, -1 lines

Summary:
Added ChoreList page with the useChores hook. Fetches chores from the API client
and renders them in a list. Wired into the router.

Tests: ✅ All 45 tests passed
Build: ✅ npm run build succeeded
Theming: ✅ No hardcoded colors — CSS custom properties only

Ready to proceed?
- [Approve for commit]
- [Request changes]
- [Abort]
